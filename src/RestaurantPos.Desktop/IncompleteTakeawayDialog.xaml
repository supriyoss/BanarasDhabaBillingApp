<Window x:Class="RestaurantPos.Desktop.IncompleteTakeawayDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Takeaway order still open"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">
  <Window.Resources>
    <Style x:Key="PrimaryDialogButton" TargetType="Button">
      <Setter Property="Background" Value="#2563EB"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#2563EB"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#1D4ED8"/><Setter TargetName="Surface" Property="BorderBrush" Value="#1D4ED8"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#1E40AF"/><Setter TargetName="Surface" Property="BorderBrush" Value="#1E40AF"/></Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Surface" Property="BorderBrush" Value="#172033"/><Setter TargetName="Surface" Property="BorderThickness" Value="2"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Border Margin="20"
          Padding="24"
          Background="White"
          BorderBrush="#E2E8F0"
          BorderThickness="1"
          CornerRadius="14">
    <Border.Effect>
      <DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.24" Color="#0F172A"/>
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Width="44" Height="44" Background="#EFF6FF" CornerRadius="22" VerticalAlignment="Top">
          <TextBlock Text="!" Foreground="#1D4ED8" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Column="1" Margin="16,0,0,0">
          <TextBlock Text="Takeaway order still open" FontSize="22" FontWeight="SemiBold" Foreground="#172033"/>
          <TextBlock Text="This order must be saved or completed before you return to Home." Margin="0,5,0,0" Foreground="#475569" FontSize="14" TextWrapping="Wrap"/>
        </StackPanel>
      </Grid>

      <Border Grid.Row="1" Margin="0,20,0,0" Padding="14" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="9">
        <TextBlock Text="Choose Save open takeaway to keep it available, or Pay / Complete to finish the order." Foreground="#1E3A8A" TextWrapping="Wrap"/>
      </Border>

      <Button x:Name="ReturnToOrderButton"
              Grid.Row="2"
              Style="{StaticResource PrimaryDialogButton}"
              Content="Return to order"
              MinWidth="132"
              Padding="16,9"
              Margin="0,24,0,0"
              HorizontalAlignment="Right"
              IsDefault="True"
              IsCancel="True"
              AutomationProperties.Name="Return to the open takeaway order"/>
    </Grid>
  </Border>
</Window>
